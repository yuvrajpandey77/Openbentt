/** Full-width hero gradient layer (sits behind header + announcement + hero copy). */
export function MarketingHeroBackdrop() {
  return (
    <div className="marketing-hero-backdrop" aria-hidden>
      <div className="marketing-hero-blobs" />
    </div>
  );
}
